export const animationDuration = 1;

export const radiusCircle = 265;

export const MOBILE_SIZE = 768;
export const TABLE_SIZE = 1280;

export const MEDIA_MOBILE = `(max-width: ${MOBILE_SIZE}px)`;
export const MEDIA_TABLET = `(max-width: ${TABLE_SIZE}px)`;
